import { Content } from 'src/types';
import { request } from '../request';

// export const sendPhone = (data: SendPhoneRequest): Promise<string> =>
//     request<string>({
//         url: '/authentication/send-confirmation-code',
//         method: 'post',
//         data,
//     }).then((response) => response);
